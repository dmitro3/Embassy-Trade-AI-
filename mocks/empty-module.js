/**
 * Generic empty module mock for Node.js modules in browser environment
 */

// Export an empty object that can be safely imported without causing errors
module.exports = {
  // This is an intentionally empty mock
};
